def split_data(*args):
    """
    Passthrough function that accepts any number of positional arguments
    and returns them as a tuple.

    Args:
        Tuple[Any]: Any number of positional arguments to be passed through.

    Returns:
        Tuple[Any]: A tuple containing all the positional arguments provided.
    """
    return args